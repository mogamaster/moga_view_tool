require "spec_helper"

describe MogaViewTool do
  it "has a version number" do
    expect(MogaViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
